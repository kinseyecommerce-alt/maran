# ── Broker ──────────────────────────────────────────────────────
kiteconnect==5.0.1
# upstox-python>=2.12.0  # optional; broker adapter uses httpx directly

# ── Market data ───────────────────────────────────────────────────
yfinance==1.4.0
# TrueData WebSocket feed (live ticks, OHLCV history, options chain)
truedata-ws
websocket-client>=0.58.0
httpx==0.28.1

# ── API server ────────────────────────────────────────────────────
fastapi==0.136.3
starlette==1.1.0
uvicorn[standard]==0.48.0
uvloop>=0.21.0           # 2-4× faster asyncio event loop (Linux/macOS only)
websockets==16.0

# ── Security ──────────────────────────────────────────────────────
slowapi==0.1.9
pyotp==2.9.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ── AI ────────────────────────────────────────────────────────────
anthropic>=0.50.0

# ── Data / Technical analysis / Charting ─────────────────────────
pandas==2.2.2
numpy==1.26.4
setuptools<60  # required to build ta==0.11.0
ta==0.11.0
matplotlib==3.10.9

# ── Scheduling ────────────────────────────────────────────────────
apscheduler==3.11.2

# ── Database (optional PostgreSQL backend) ───────────────────────
psycopg2-binary>=2.9.0   # only needed when DATABASE_URL=postgres://... is set

# ── Utilities ────────────────────────────────────────────────────
python-dotenv==1.0.1
python-telegram-bot==21.3
pydantic==2.13.4
pydantic-settings==2.14.1
loguru==0.7.2
aiofiles==25.1.0

# ── Swagger UI (served locally, no CDN dependency) ────────────────
swagger-ui-bundle==1.1.0

# ── Testing (not needed in production) ───────────────────────────
playwright==1.52.0
scikit-learn>=1.3.0
scipy>=1.11.0

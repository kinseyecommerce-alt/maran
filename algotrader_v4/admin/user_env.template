# AlgoTrader Pro v4 — User: {username}
# Created: {created_at}
#
# ──────────────────────────────────────────────────────────────
# STEP 1 — Fill in YOUR Zerodha Kite Connect credentials.
#
# Create your own Kite Connect app at:
#   https://developers.kite.trade/  →  My Apps  →  Create App
#
# Set the Redirect URL in your Kite app to:
#   http://YOUR_SERVER_IP_OR_DOMAIN:{port}/auth/kite/callback
# ──────────────────────────────────────────────────────────────
KITE_API_KEY=
KITE_API_SECRET=
KITE_REDIRECT_URL=http://YOUR_SERVER:{port}/auth/kite/callback

# Optional: enable morning auto-login via TOTP (Zerodha 2FA seed).
# Leave blank to do manual OAuth each morning instead.
KITE_USER_ID=
KITE_PASSWORD=
KITE_TOTP_SECRET=

# Optional: Claude AI trade gate (highly recommended for LIVE mode).
# Get your key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional: Telegram alerts
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ──────────────────────────────────────────────────────────────
# GENERATED BY PROVISIONER — do not edit below this line
# ──────────────────────────────────────────────────────────────
ADMIN_USERNAME={username}
ADMIN_PASSWORD_HASH={password_hash}
JWT_SECRET_KEY={jwt_secret}

# Paths (isolated per user — do not change)
DATABASE_PATH={db_path}
KITE_ACCOUNTS_FILE={kite_accounts_file}
ADAPTIVE_DATA_DIR={adaptive_data_dir}

# Trading defaults (can be changed via dashboard Settings)
TRADING_MODE=PAPER
TOTAL_CAPITAL={capital}
INTRADAY_CAPITAL_PCT=30.0
SWING_CAPITAL_PCT=20.0
FNO_CAPITAL_PCT=15.0
FUTURES_CAPITAL_PCT=10.0
MAX_DAILY_LOSS=5000.0
MAX_OPEN_POSITIONS=5
USE_CLAUDE_TRADE_GATE=false

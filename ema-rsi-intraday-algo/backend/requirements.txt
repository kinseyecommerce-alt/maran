# ── Phase 1 runtime (strategy core) ──
pydantic>=2.6
pydantic-settings>=2.2
PyYAML>=6.0

# ── Phase 2+ (declared now; not required to run Phase-1 tests) ──
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
alembic>=1.13
asyncpg>=0.29
redis>=5.0
pandas>=2.2
numpy>=1.26
structlog>=24.1
httpx>=0.27
apscheduler>=3.10
dramatiq[redis]>=1.16
argon2-cffi>=23.1
python-jose[cryptography]>=3.3
kiteconnect>=5.0

# ── Live service (Kite auth: env token, or headless TOTP auto-login) ──
playwright>=1.40
pyotp>=2.9

# ── Dev / test ──
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.3
mypy>=1.9
